# Generate a random access key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PORTFOLIO_ADMIN_TOKEN=

# Connect a PRIVATE Vercel Blob store. Use the generated token or OIDC store connection.
BLOB_READ_WRITE_TOKEN=
BLOB_STORE_ID=
